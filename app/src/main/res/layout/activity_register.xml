<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    android:id="@+id/register_scrollview"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:background="@android:color/white"
    android:orientation="horizontal"
    tools:context=".register.presentation.RegisterActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <fragment
            android:id="@+id/register_fragment"
            android:layout_marginTop="24dp"
            android:name="com.example.instagramremake.register.presentation.RegisterPhotoFragment"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:layout="@layout/fragment_register_photo" />
    </LinearLayout>

</ScrollView>